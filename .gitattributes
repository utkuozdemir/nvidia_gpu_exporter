# The captures are raw recorded nvidia-smi output and the goldens are exact
# expected scrapes: both are compared byte for byte by the integration tests,
# so they must never be CRLF-converted on checkout (core.autocrlf on Windows
# would otherwise break the whole suite).
internal/captures/*.txt text eol=lf
integration/testdata/*.metrics text eol=lf
